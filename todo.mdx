ui/docs
ui/docs/ui/breadcrumb-nav
docs
blog
ui/console

buy buttons all do different things

support links are broken

deploy callback sign in -> continue callback

scripts + admin ability to run

onboarding

bitbucket

gitlab

resend magic link

sign in out

seed payload onInit

payload cms link to admin dashboard
payload cms manage branding, logo, favicon, polar, lemonsqueezy, etc
payload cms manage users, roles, permissions
payload cms manage teams, projects, tasks, etc
payload cms manage settings, general, notifications, etc

