"use client";

/**
 * Builder.io integration stub.
 * The original builder-io module references components that don't exist.
 * TODO: Implement actual Builder.io rendering when needed.
 */
export function RenderBuilderContent({
  content,
  model,
}: {
  content: unknown;
  model?: string;
}) {
  return null;
}
